<script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-6408853-4', 'auto');
    ga('send', 'pageview');

</script>

<% if signed_in? 	  %>

    <body >
        <%= render 'layouts/topbar' %>
        <div id="imagenprincipal" style="margin-top: 50px ">


          <div class="col-sm-offset-1 col-sm-10 col-xs-10" style="margin-top: 5px; margin-bottom: 5px">
            <% [:notice, :error, :alert].each do |level| %>
                <% unless flash[level].blank? %>
                    <div data-alert="alert" class="alert-message <%= flash_class(level) %> fade in">
                      <a class="close" href="#">×</a>
                      <%= content_tag :p, flash[level] %>
                    </div>
                <% end %>
            <% end %>
          </div>
          <%= yield %>
        </div>
    </body>

<%  else %>
    <body   style="background-color: #e6e6e5">
        <!-- Page content -->
        <div class="container" >
          <div class="hidden-xs">
            <%= render 'layouts/topbarlo' %>
          </div>
          <div class="visible-xs">
            <a id="menu-toggle" href="#" class="btn btn-default"><i class="icon-reorder"></i></a>
          </div>
          <!-- Keep all page content within the page-content inset div! -->
          <div class="col-sm-offset-1 col-sm-10 col-xs-10" style="margin-top: 5px; margin-bottom: 5px">
            <% [:notice, :error, :alert].each do |level| %>
                <% unless flash[level].blank? %>
                    <div data-alert="alert" class="alert-message <%= flash_class(level) %> fade in">
                      <a class="close" href="#">×</a>
                      <%= content_tag :p, flash[level] %>
                    </div>
                <% end %>
            <% end %>
          </div>
          <%= yield %>
          <%= render "layouts/footer" %>
        </div>
    </body>


<% end %>